import BaseText from "@/components/BaseText";
import BaseView from "@/components/BaseView";

export default function Index() {
  return (
    <BaseView
      style={{
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <BaseText>This screen is currently under development.</BaseText>
    </BaseView>
  );
}
